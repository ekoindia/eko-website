{% extends "./base.njk" %}

{% block seo %}
	<title>{{ title }} | {{ website.title }}</title>
	<meta name="description" content="{{ description or page.excerpt | trim }}">
	{# <meta name="keywords" content="{{ website.keywords | trim }}"> #}

	{# OpenGraph (Facebook) Social Card #}
	<meta property="og:image" content="{{ env.BASE_URL }}{{ image }}">
	<meta property="og:title" content="{{ title }} | {{ website.title }}">
	<meta property="og:description" content="{{ description or page.excerpt | trim }}">

	{# Twitter Social Card #}
	<meta name="twitter:title" content="{{ title }} | {{ website.title }}">
	<meta name="twitter:description" content="{{ description or page.excerpt | trim }}">
	<meta name="twitter:image" content="{{ env.BASE_URL }}{{ image }}">
{% endblock %}

{% block custom_style %}
	{{ css_blog | cssmin | safe }}
{% endblock %}

{% block main %}
	<section>
        <h1>{{ title }}</h1>
        <h6>
			<time>{{ date | dateFormat('dd MMM yyyy') }}</time>
			{% for item in tags %}
				<a class="blogtag" href="/blog/{{item}}/">{{ item }}</h1></li></h4>
			{% endfor %}
		</h6>
        <img class="hero" src="{{ image | url }}" alt="{{ image }}">
        <p>{{ content | safe }}</p>
    </section>
{% endblock %}
