{% extends "./base.njk" %}

{% block main %}
    <section>
        <h2>{{ title }}</h2>
        <h3>{{ date | readableDate }}</h3>
        <img src="{{ image | url }}" alt="{{ image }}">
        <p>{{ content | safe }}</p>
    </section>
{% endblock %}