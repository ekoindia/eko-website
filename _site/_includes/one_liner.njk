<center>
    <div class="one-liner">
        <img src="{{ product.one_liner.icon }}" alt="{{ product.one_liner.title }}">
        <h1>{{ product.one_liner.title }}</h1>
        <span class="h2-border"></span>
        <p>{{ product.one_liner.text }}</p>
    </div>
</center>
</div>