---
permalink: /blogs/
---

{% extends "./_layouts/pageTemplates/base.njk" %}

{% block main %}
    {% set blogslist = collections.blogs %}
    {# {% include "blogslist.njk" %} #}
    <div class="container">
        <header>
            <h1>Blog Posts</h1>
            <span class="h2-border"></span>
        </header>
    {# <ul>
    {% for blog in blogslist %}
        <li>
        <strong>
            <a href="{{ blog.url | url }}">{{ blog.data.title }}</a>
        </strong>
        -
        <time datetime="{{ blog.date | htmlDateString }}">{{ blog.date | readableDate }}</time>
        -
        <span>{{ blog.data.description }}</span>
        </li>
    {% endfor %}
    </ul> #}
    
        <div class="band">
            {% for blog in blogslist %}
            <div class="item">
                <a href="{{ blog.url | url }}" class="card">
                <div class="thumb" style="background-image: url({{ blog.data.featuredImage }})"></div>
                <article>
                    <h1>{{ blog.data.title }}</h1>
                    <span><time datetime="{{ blog.date | htmlDateString }}">{{ blog.date | readableDate }}</time></span>
                    <p>{{ blog.data.description }}</p>
                </article>
                </a>
            </div>
            {% endfor %}
        </div>
    </div>
    {% sectionSeparator %}
{% endblock %}